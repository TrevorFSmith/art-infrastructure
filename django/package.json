{
  "name": "art-infrastructure",
  "version": "0.1.0",
  "description": "For development environment, the app requires `coffee` npm package. To install it on Ubuntu/Debian:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Trevor F. Smith <subs@trevors.smith.name>",
  "license": "MIT",
  "devDependencies": {
    "coffeescript": "^1.12.7",
    "phantomjs-prebuilt": "^2.1.16",
    "selenium": "^2.20.0"
  }
}
